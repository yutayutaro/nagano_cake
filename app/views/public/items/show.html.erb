<%=image_tag @item.image %>
<%= @item.name %>
<%= @item.introductiion %>
¥<%= @item.price %>
<%= form_with model: @cart_itam, url: cart_items_path,method: :post do |f| %>
<%= f.hidden_field :item_id, :value => @item.id %>
<%= f.select :amount, *[1..10],prompt: "個数選択" %>
<%= f.submit 'カートに入れる', class: "btn btn-success" %>
<% end %>
